{
    "name": "corepine/modal",
    "description": "Reusable Alpine + Livewire modal stack for Corepine packages.",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "laravel",
        "livewire",
        "alpine",
        "modal",
        "corepine"
    ],
    "authors": [
        {
            "name": "Namu Makwembo",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.2|^8.3|^8.4",
        "corepine/support": "^0.1.3 || dev-main",
        "laravel/framework": "^11.0|^12.0|^13.0",
        "livewire/livewire": "^3.7|^4.0"
    },
    "require-dev": {
        "orchestra/testbench": "^9.0|^10.0|^11.0",
        "pestphp/pest": "^2.34|^3.0",
        "pestphp/pest-plugin-laravel": "^2.4|^3.0",
        "christophrumpel/missing-livewire-assertions": "^2.8|^3.0"
    },
    "autoload": {
        "psr-4": {
            "Corepine\\Modal\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Corepine\\Modal\\Tests\\": "tests/",
            "Workbench\\": "workbench/"
        }
    },
    "scripts": {
        "post-autoload-dump": [
            "@clear",
            "@prepare"
        ],
        "clear": "@php vendor/bin/testbench package:purge-skeleton --ansi",
        "prepare": "@php vendor/bin/testbench package:discover --ansi",
        "build": "@php vendor/bin/testbench workbench:build --ansi",
        "serve": [
            "Composer\\Config::disableProcessTimeout",
            "@build",
            "@php vendor/bin/testbench serve --ansi"
        ],
        "test": [
            "@php vendor/bin/pest"
        ]
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Corepine\\Modal\\ModalServiceProvider"
            ],
            "aliases": {
                "Modal": "Corepine\\Modal\\Facades\\Modal"
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
